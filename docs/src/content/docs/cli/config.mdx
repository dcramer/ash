---
title: ash config
description: Configuration management commands
sidebar:
  order: 7
---

Manage Ash configuration.

## Usage

```bash
uv run ash config COMMAND [OPTIONS]
```

## Commands

### init

Create a new configuration file:

```bash
uv run ash config init
```

Creates `~/.ash/config.toml` with default values.

### show

Display current configuration:

```bash
uv run ash config show
```

Options:
| Option | Description |
|--------|-------------|
| `--config`, `-c` | Path to config file |

### validate

Validate configuration file:

```bash
uv run ash config validate
```

Checks:
- TOML syntax
- Required fields
- Type correctness
- API key availability

## Setup Wizard

For interactive configuration, use the setup wizard:

```bash
uv run ash setup
```

Options:
| Option | Description |
|--------|-------------|
| `--section` | Configure specific section |
| `--reconfigure` | Reset and reconfigure |

Configure a specific section:

```bash
uv run ash setup --section models
uv run ash setup --section telegram
```

## Examples

Create initial config:

```bash
uv run ash config init
```

Check configuration:

```bash
uv run ash config validate
```

View current settings (redacts secrets):

```bash
uv run ash config show
```
